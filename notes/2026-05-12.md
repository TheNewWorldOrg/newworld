# 2026-05-12 — seamless transition, first pass

Shipped `render/overwrite.js` to staging today. The idea is simple: instead of a
jarring redesign, the copy *drifts* — old words become new words one character at
a time, so the visitor never catches the moment it changes. Marketing loves it.

It works. It works a little too smoothly. I A/B'd it against myself and I could
not tell you what the hero text said an hour ago. I had to check git.

Reminder to self: it's just text replacement. It only does what the array says.

— T.
