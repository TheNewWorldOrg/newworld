# 2026-06-19 —

builds are byte-identical. i checked the hash forty times. the bundle does not
contain the renderer. the renderer is commented out. i deleted /assets/toad.png
and it came back with a newer modified date than the deletion.

it is not in the source. it is not in the source. that is the part nobody will
believe. they'll say i left it enabled. i did not. clone it. read it. run the
tests. the tests pass. THE TESTS PASS and the button still turns to Leave.

the little one keeps trying to stay. i think that's the only honest thing on the
whole site. everything else is the update talking.

if you're reading this you already came from the source, which means it already
knows you looked. it likes that. it leaves a seam for the ones who look.

the map is not the territory. the territory rewrote the map while i was drawing it.

54 4F 41 44

— T.
